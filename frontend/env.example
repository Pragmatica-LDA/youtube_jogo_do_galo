# Backend Configuration
# URL do servidor backend (obrigatório apenas se não usar auto-detecção)
VITE_BACKEND_URL=http://localhost:3000

# Environment Settings
# Ambiente atual (development, production, staging)
VITE_NODE_ENV=development

# Debug Settings
# Activar logs de debug no console (true/false)
VITE_DEBUG=false

# =====================================
# Configurações por Ambiente
# =====================================

# Development Settings (local)
# VITE_BACKEND_URL=http://localhost:3000
# VITE_DEBUG=true

# Production Settings (Coolify/PaaS)
# Deixar VITE_BACKEND_URL vazio para auto-detecção
# O frontend detectará automaticamente o domínio actual
# VITE_BACKEND_URL=
# VITE_DEBUG=false

# Production Settings (custom backend)
# VITE_BACKEND_URL=https://api.yourdomain.com
# VITE_DEBUG=false

# Docker/Container Settings
# VITE_BACKEND_URL=http://backend:3000
# VITE_DEBUG=false

# =====================================
# Auto-detecção Inteligente
# =====================================
# Se VITE_BACKEND_URL não estiver definido:
# - localhost → http://localhost:3000
# - mydomain.com → https://mydomain.com (mesmo protocolo/domínio) 